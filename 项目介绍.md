# 门店后台管理系统项目介绍

## 1. 项目概述

本项目是一个面向门店经营场景的后台管理系统，当前重构项目目录为 `beauty-console`。系统主要用于管理门店用户、员工、客户、服务项目、服务项目耗材关系、预约、服务订单、支付记录、库存物品和库存流水。

项目采用前后端分离思路，后端提供 REST API，前端通过登录接口获取 JWT token，后续访问后台接口时携带 token 完成身份认证。

当前后端重点目标是：

- 拆分清晰的 Maven 多模块结构。
- 使用 DTO 接收请求参数，使用 VO 返回前端数据。
- 使用 Entity 对应数据库表。
- 使用 MyBatis-Plus 快速生成基础 CRUD 骨架。
- 在关键业务中补充校验、事务、金额计算、库存变更和登录认证。

## 2. 技术栈

| 技术 | 作用 |
|---|---|
| Java 17 | 后端开发语言 |
| Spring Boot 3.5.16 | 后端主框架 |
| MyBatis-Plus 3.5.16 | ORM 和基础 CRUD |
| MySQL | 业务数据库 |
| Redis | Spring Cache 缓存后端 |
| Spring Cache | 缓存服务项目耗材关系等高频配置 |
| EasyExcel | 库存物品 Excel 导出 |
| Maven | 多模块项目构建 |
| Lombok | 简化实体、DTO、VO 样板代码 |
| Swagger Annotations | 接口注解说明 |
| JWT | 登录认证 token |
| BCrypt | 密码哈希存储与校验 |

## 3. 模块结构

```text
beauty-console
├── pom.xml
├── store-common
│   ├── common
│   ├── exception
│   ├── properties
│   └── util
├── store-pojo
│   ├── dto
│   ├── entity
│   ├── enums
│   └── vo
└── store-server
    ├── config
    ├── controller
    ├── handler
    ├── interceptor
    ├── mapper
    ├── security
    ├── service
    ├── service/impl
    ├── excel
    └── resources/mapper
```

## 4. 各模块职责

| 模块 | 职责 |
|---|---|
| `store-common` | 存放通用返回对象、分页结果、上下文、异常、JWT 工具、配置属性等公共能力 |
| `store-pojo` | 存放 DTO、VO、Entity、Enum，不直接写业务逻辑 |
| `store-server` | 存放 Controller、Service、Mapper、拦截器、配置类和启动类，是主要业务模块 |

## 5. 核心业务模块

| 模块 | 说明 |
|---|---|
| 用户管理 | 后台账号登录、分页查询、新增、修改、启停用，角色覆盖六类后台角色 |
| 员工管理 | 维护门店员工资料，供账号、预约、订单服务人员关联 |
| 客户管理 | 维护客户姓名、电话、性别、生日、等级、来源等资料 |
| 服务项目 | 维护门店可售卖或可预约的服务项目 |
| 服务项目耗材关系 | 维护服务项目完成时需要消耗的库存 SKU 和单次消耗数量 |
| 预约管理 | 创建预约、维护预约项目明细、查询预约列表和详情 |
| 服务订单 | 新建订单、从预约生成订单、维护订单明细、校验订单金额和支付状态 |
| 支付记录 | 新增收款、退款、作废记录，并同步反写订单已付金额、欠款金额、支付状态 |
| 库存物品 | 维护库存 SKU、当前库存、安全库存、成本价、供应商，并支持 Excel 导出 |
| 库存流水 | 记录入库、出库、盘点、报损、退货，并同步更新库存数量 |

## 6. 数据库核心表

| 表名 | 说明 |
|---|---|
| `sys_user` | 后台登录账号 |
| `staff_member` | 员工档案 |
| `customer_profile` | 客户资料 |
| `service_project` | 服务项目 |
| `service_project_inventory` | 服务项目耗材关系 |
| `appointment` | 预约主表 |
| `appointment_item` | 预约项目明细 |
| `service_order` | 服务订单主表 |
| `service_order_item` | 服务订单项目明细 |
| `payment_record` | 收款、退款、作废记录 |
| `inventory_sku` | 库存物品 |
| `inventory_stock_log` | 库存变动流水 |

## 7. 接口约定

后台接口统一使用 `/admin` 前缀，登录接口除外也在 `/admin/users/login` 下。

统一返回格式：

```json
{
  "code": 1,
  "msg": null,
  "data": {}
}
```

分页返回格式：

```json
{
  "code": 1,
  "msg": null,
  "data": {
    "total": 100,
    "records": []
  }
}
```

登录后请求头：

```http
token: Bearer <登录返回的token>
```

或者在当前拦截器兼容逻辑下：

```http
token: <登录返回的token>
```

请求头名称由配置控制：

```yaml
sky:
  jwt:
    admin-token-name: token
```

## 8. 典型业务流程

### 8.1 登录流程

```text
前端提交 username/password
-> SysUserController
-> SysUserService
-> 查询 sys_user 和员工信息
-> 校验密码
-> 生成 JWT
-> 返回 LoginUserVO
```

### 8.2 预约转订单流程

```text
查询预约主表
-> 查询预约明细
-> 生成订单号
-> 复制必要客户、员工、服务时间信息
-> 根据预约明细生成订单明细
-> 汇总金额
-> 校验订单金额
-> 保存订单主表
-> 保存订单明细
```

### 8.3 支付记录流程

```text
新增支付记录
-> 查询订单
-> 校验订单是否存在、是否取消
-> 校验支付状态和金额
-> 保存 payment_record
-> 根据成功收款或退款反写订单金额
-> 刷新 payStatus 和 debtStatus
```

### 8.4 库存流水流程

```text
新增库存流水
-> 查询库存物品并使用 FOR UPDATE 加行锁
-> 计算变动前库存
-> 按 changeType 计算变动后库存
-> 校验不能出现负库存
-> 更新 inventory_sku.quantity
-> 写入 inventory_stock_log
```

### 8.5 订单完成扣库存流程

```text
完成服务订单
-> 锁定 service_order 防止重复完成
-> 查询订单明细
-> 根据 service_project_id 查询启用的耗材关系配置
-> 计算扣减数量 = 订单明细数量 * consume_quantity
-> 调用库存流水服务出库
-> 更新订单状态为已完成
```

## 9. 当前运行配置

默认服务端口：

```yaml
server:
  port: 8080
```

默认数据库：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_platform
    username: root
    password: 12345678
```

默认 JWT 配置：

```yaml
sky:
  jwt:
    admin-secret-key: itcast
    admin-ttl: 7200000
    admin-token-name: token
```

默认 Redis 配置：

```yaml
spring:
  cache:
    type: redis
    redis:
      time-to-live: 10m
      cache-null-values: false
  data:
    redis:
      host: localhost
      port: 6379
```

## 10. 启动和验证

在项目根目录执行：

```bash
mvn -pl store-server -am test
```

启动后端：

```bash
mvn -pl store-server -am spring-boot:run
```

默认登录账号：

```text
admin / 123456      超级管理员
manager / 123456    店长
staff / 123456      普通员工
inventory / 123456  库存管理员
finance / 123456    财务/收银
readonly / 123456   只读
```

如果不想重跑全量种子数据，可以只执行 `store-server/src/main/resources/sql/patch_test_role_accounts.sql` 补齐这些账号。

## 11. 当前完成情况

已完成：

- 多模块结构拆分。
- DTO、VO、Entity、Enum 基础代码补齐。
- Controller 基础接口实现。
- 登录认证链路。
- JWT 拦截器接入。
- BCrypt 密码校验和旧明文密码兼容升级。
- 分页查询空条件处理。
- MyBatis-Plus 物理分页插件接入，分页参数已生效。
- 订单金额强校验。
- 从预约生成订单。
- 支付记录反写订单金额。
- 库存流水同步更新库存。
- 库存扣减使用数据库行锁避免并发错账。
- 服务项目耗材关系表和接口。
- 订单完成时按耗材关系自动扣减库存。
- 已完成服务订单取消时按原始出库流水回滚库存。
- 服务订单 Controller 已下沉复杂业务到 Service。
- 六类角色权限控制。
- 前端菜单按当前登录角色过滤，工作台只请求当前角色有权限的数据。
- Redis + Spring Cache 缓存服务项目耗材关系。
- EasyExcel 导出库存物品。
- Controller、权限、登录、库存、订单扣库存等单元测试通过。

待完善：

- 更多真实联调和接口集成测试。
- Excel 导入能力，例如批量导入库存物品或服务项目耗材关系。
- Redis 不可用时的降级策略和运维监控。
- 更多统计报表，例如库存消耗、低库存预警、员工业绩。

## 12. 前端项目位置

当前前端项目已整理到：

```text
D:\_study\java\后台管理系统\beauty-console-web
```

说明：

- 前端由苍穹外卖 Vue2 + TypeScript 后台模板改造而来。
- 当前已适配美容门店后台的登录、仪表盘、客户、员工、预约、服务订单、服务项目、库存、耗材关系、库存流水、支付记录和用户管理。
- 前端本地开发默认转发到后端 `http://localhost:8080`。
- 本地访问地址默认使用 `http://localhost:8888/`。
- 前端目录与后端 `beauty-console` 并列放在 `D:\_study\java\后台管理系统` 下，便于后续统一管理。
## 本轮新增能力

- 库存耗材：新增 15 条真实美容门店耗材数据，并同步到本地 `db_platform`。
- 库存并发：库存扣减改为“旧数量条件更新 + 失败重读重试”，降低并发扣减覆盖风险。
- 权限点配置：账号权限从固定角色扩展为数据库权限点，支持用户级权限覆盖。
- 前端权限：登录后按 `permissions` 权限码过滤菜单、工作台入口和资源按钮；账号权限页可直接勾选权限点。
- 集成测试：新增 Spring Boot + MockMvc + H2 集成测试，覆盖登录、分页、库存扣减和越权拦截。

相关 SQL：

```text
store-server/src/main/resources/sql/patch_role_permissions.sql
store-server/src/main/resources/sql/patch_permission_points.sql
store-server/src/main/resources/sql/patch_inventory_consumables.sql
```

验证结果：

```text
mvn test                              -> 45 tests passed
npm run build (beauty-console-web)     -> build passed, only legacy Sass/asset-size warnings
```
